basePath: /
definitions:
  book.BookDTO:
    properties:
      author_names:
        items:
          type: string
        type: array
      cover_url:
        type: string
      description:
        type: string
      key:
        type: string
      title:
        type: string
    type: object
  catalog.BookWithCost:
    properties:
      CostEUR:
        type: number
      CostUSD:
        type: number
      authorNames:
        items:
          type: string
        type: array
      coverURL:
        type: string
      description:
        type: string
      errorMessage:
        type: string
      failed:
        type: boolean
      failedStage:
        type: string
      id:
        type: string
      key:
        type: string
      language:
        $ref: '#/definitions/primitive.Language'
      parsed:
        type: boolean
      prepared:
        type: boolean
      retryDisabled:
        description: RetryDisabled marks Failed as permanent — see book.RecordPermanentFailure.
        type: boolean
      scriptGenerated:
        type: boolean
      title:
        type: string
      tokenUsage:
        $ref: '#/definitions/primitive.TokenUsage'
      ttsgenerated:
        type: boolean
      uploaded:
        type: boolean
    type: object
  primitive.Language:
    enum:
    - fr
    - en
    type: string
    x-enum-varnames:
    - French
    - English
  primitive.ModelUsage:
    properties:
      input_tokens:
        type: integer
      output_tokens:
        type: integer
      total_tokens:
        type: integer
    type: object
  primitive.TokenUsage:
    additionalProperties:
      $ref: '#/definitions/primitive.ModelUsage'
    type: object
  primitive.UserRole:
    enum:
    - basic
    type: string
    x-enum-varnames:
    - Basic
  user.LoginRequestDTO:
    properties:
      email:
        maxLength: 200
        type: string
      password:
        maxLength: 100
        minLength: 12
        type: string
    required:
    - email
    - password
    type: object
  user.RegisterRequestDTO:
    properties:
      email:
        maxLength: 200
        type: string
      firstname:
        maxLength: 100
        type: string
      lastname:
        maxLength: 100
        type: string
      password:
        maxLength: 100
        minLength: 12
        type: string
    required:
    - email
    - firstname
    - lastname
    - password
    type: object
  user.UpdateUserRequestDTO:
    properties:
      firstname:
        maxLength: 100
        type: string
      language:
        allOf:
        - $ref: '#/definitions/primitive.Language'
        enum:
        - en
        - fr
      lastname:
        maxLength: 100
        type: string
    required:
    - firstname
    - language
    - lastname
    type: object
  user.User:
    properties:
      createdAt:
        type: string
      deletedAt:
        type: string
      email:
        type: string
      firstname:
        type: string
      id:
        type: string
      language:
        $ref: '#/definitions/primitive.Language'
      lastname:
        type: string
      passwordHash:
        items:
          format: int32
          type: integer
        type: array
      role:
        $ref: '#/definitions/primitive.UserRole'
      updatedAt:
        type: string
    type: object
host: localhost:3000
info:
  contact: {}
  description: Queue-driven pipeline API that converts EPUBs into narrated audio.
  title: Book Narration API
  version: "1.0"
paths:
  /books/:
    get:
      description: Returns up to 100 books saved in the local database
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/catalog.BookWithCost'
            type: array
        "500":
          description: Internal Server Error
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: List saved books
      tags:
      - books
  /books/{query}:
    get:
      description: Fetch a book from Google Books by its volume ID
      parameters:
      - description: Google Books volume ID
        in: path
        name: query
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/book.BookDTO'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Get book by key
      tags:
      - books
  /books/{query}/retry:
    post:
      description: Re-publishes the book onto the queue for the stage it last failed
        at, and clears the failure
      parameters:
      - description: Book ID (UUID)
        in: path
        name: query
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Retry a failed book
      tags:
      - books
  /books/audio/{query}:
    get:
      description: Streams the synthesized audio file for the given book ID from MinIO
      parameters:
      - description: Book ID (UUID)
        in: path
        name: query
        required: true
        type: string
      produces:
      - application/octet-stream
      responses:
        "200":
          description: OK
          schema:
            type: file
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Stream audio book
      tags:
      - books
  /books/search/{query}:
    get:
      description: Search for books on Google Books by title or keyword
      parameters:
      - description: Search query
        in: path
        name: query
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/book.BookDTO'
            type: array
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Search books
      tags:
      - books
  /users:
    get:
      description: Fetch all non-deleted user records
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            items:
              $ref: '#/definitions/user.User'
            type: array
      summary: List users
      tags:
      - users
  /users/{id}:
    delete:
      description: Soft-delete a user account; requires a bearer token for the same
        user; the account can no longer log in
      parameters:
      - description: User UUID
        in: path
        name: id
        required: true
        type: string
      responses:
        "204":
          description: No Content
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Delete user
      tags:
      - users
    get:
      description: Fetch a user record by their UUID
      parameters:
      - description: User UUID
        in: path
        name: id
        required: true
        type: string
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/user.User'
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Get user
      tags:
      - users
    patch:
      consumes:
      - application/json
      description: Update a user's firstname and lastname; requires a bearer token
        for the same user
      parameters:
      - description: User UUID
        in: path
        name: id
        required: true
        type: string
      - description: Update payload
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/user.UpdateUserRequestDTO'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/user.User'
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "401":
          description: Unauthorized
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            additionalProperties:
              type: string
            type: object
        "404":
          description: Not Found
          schema:
            additionalProperties:
              type: string
            type: object
      security:
      - BearerAuth: []
      summary: Update user
      tags:
      - users
  /users/login:
    post:
      consumes:
      - application/json
      description: Authenticate with email and password; returns JWT access and refresh
        tokens
      parameters:
      - description: Login credentials
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/user.LoginRequestDTO'
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
        "403":
          description: Forbidden
          schema:
            type: string
      summary: Login user
      tags:
      - users
  /users/register:
    post:
      consumes:
      - application/json
      description: Create a new user account; returns JWT access and refresh tokens
      parameters:
      - description: Registration payload
        in: body
        name: body
        required: true
        schema:
          $ref: '#/definitions/user.RegisterRequestDTO'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            additionalProperties:
              type: string
            type: object
        "400":
          description: Bad Request
          schema:
            additionalProperties:
              type: string
            type: object
      summary: Register user
      tags:
      - users
securityDefinitions:
  BearerAuth:
    description: Type "Bearer" followed by a space and the JWT access token.
    in: header
    name: Authorization
    type: apiKey
swagger: "2.0"
